{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Electron: npm run start",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run start",
      "cwd": "${workspaceFolder}/app"
    }
  ]
}
